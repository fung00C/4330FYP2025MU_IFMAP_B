# Web API
fastapi==0.115.5          # 最新穩定版支援 Pydantic v2，含 lifespan 等特性 [web:34]
uvicorn[standard]==0.30.6 # 開發用 ASGI 伺服器，含自動重載/uvloop 可選 [web:34]

# Scheduling
APScheduler==3.10.4       # 與 asyncio 相容的排程器，用於每日任務

# Data & Finance
pandas==2.3.3             # 與 Python 3.11+ 和新 yfinance 相容的穩定版 [web:38]
numpy==1.26.4             # 與 pandas 2.3 相容且穩定的版本線 [web:38]
yfinance==0.2.66          # 2025 年較新版本，修復多項相依性問題 [web:36]
requests>=2.31.0          # yfinance 依賴，明確標註以避免舊版 [web:31]

# Settings（可選，若採用 .env/設定管理）
pydantic-settings==2.6.1  # 若你使用 BaseSettings 管理環境變數與設定 [web:34]

# CSV/Parquet（可選：若需更快 CSV/Parquet）
pyarrow==17.0.0           # pandas 可選依賴，加速讀寫（可移除）
